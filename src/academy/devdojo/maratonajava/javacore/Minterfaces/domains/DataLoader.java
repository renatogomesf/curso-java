package academy.devdojo.maratonajava.javacore.Minterfaces.domains;

// interface não é classe.
//interface foram criada para obrigar uma classe a implementar um metodo ou seguir uma estrutura.
public interface DataLoader {

    // nas interfaces, todos os metodos são public e abstract e não possuem corpo. ou seja, não precisa colocar eles no metodo.
    void load();
}
